{
  "name": "@brycepelletier/agent-env-mcp",
  "version": "0.4.1",
  "description": "Hardened MCP gateway for reusable containerized software-engineering environments.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brycepelletier/agent-env-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/brycepelletier/agent-env-mcp/issues"
  },
  "homepage": "https://github.com/brycepelletier/agent-env-mcp#readme",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "bin": {
    "agent-env-mcp": "index.mjs"
  },
  "files": [
    "LICENSE",
    "index.mjs",
    "runtime/**",
    "README.md"
  ],
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "link": "node --check ./index.mjs && node --check ./runtime/helper.mjs && node --check ./runtime/lifecycle.mjs && npm install && npm link",
    "unlink": "npm uninstall --global @brycepelletier/agent-env-mcp",
    "test": "node --test",
    "validate": "node --check ./index.mjs && node --check ./runtime/helper.mjs && node --check ./runtime/lifecycle.mjs && npm test && npm pack --dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "zod": "^3.22.0"
  }
}
